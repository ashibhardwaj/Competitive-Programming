#include <iostream>
using namespace std;
 
int main() {
	// your code goes here
	int n, ans=0;
	cin>>n;
	int p[n], v[n], t[n], a[n];
	for(int i=0; i<n; i++)
	{
		cin>>p[i]>>v[i]>>t[i];
		a[i]=p[i]+v[i]+t[i];
	}
	for(int i=0; i<n; i++)
		if(a[i]>1)
			ans++;
	cout<<ans;
	return 0;
}
