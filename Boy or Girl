#include <bits/stdc++.h>
using namespace std;

int main() {
	// your code goes here
	string s;
	cin>>s;
	int n=s.length();
	set<char>se;
	for(int i=0; i<n; i++)
	{
		se.insert(s[i]);
	}
	if(se.size()%2==0)
	cout<<"CHAT WITH HER!";
	else
	cout<<"IGNORE HIM!";
	return 0;
}
