#include <bits/stdc++.h>
using namespace std;
 
int main() {
	long long n;
	map <string, long long> m;
	string s;
	cin>>n;
	for(long long i=0; i<n; i++)
	{
		cin>>s;
		if(m[s]==0)
		{
			cout<<"OK"<<endl;
		}
		else
		{
			cout<<s<<m[s]<<endl;
		}
		m[s]++;
	}
}
